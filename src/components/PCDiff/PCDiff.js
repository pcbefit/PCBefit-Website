import React, {Fragment} from 'react';
import {
	Segment,
} from 'semantic-ui-react';

export const PCDiff = () => {
	return <Fragment>
		<Segment inverted
		         textAlign='center'
		         style={{minHeight: 700, padding: '1em 0em'}}
		         vertical>
			PCDiff container
		</Segment>
	</Fragment>;
};
